DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app
DATABASE_URL_DIRECT=
DATABASE_POOL_MAX=

# Optional PostgreSQL TLS policy. Prefer a verified CA in production.
# DATABASE_CA_CERT accepts a PEM with literal \\n separators; DATABASE_CA_CERT_FILE is useful locally.
DATABASE_CA_CERT=
DATABASE_CA_CERT_FILE=
# Leave empty to honor the provider URL. Use `off` only locally; `relaxed` disables certificate verification.
DATABASE_SSL=

# Public application origin used in invitation links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional comma-separated hosts accepted by the remote MCP endpoint in addition to the app/Vercel hosts.
MCP_ALLOWED_HOSTS=

# Clerk proves identity; app_user remains authoritative for role and active status.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# One-time production bootstrap used by `pnpm auth:bootstrap`.
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_NAME=

# Enables the passwordless role selector only outside production.
# Production ignores this value and uses src/features/auth/adapter.ts.
ALLOW_UNSAFE_LOCAL_PREVIEW=true
